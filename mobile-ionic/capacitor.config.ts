import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.aprendizaje.arriendocanchas',
  appName: 'Arriendo de Canchas',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
