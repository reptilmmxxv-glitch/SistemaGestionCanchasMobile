[build]
  base = "mobile-ionic"
  command = "npm install && npm run build"
  publish = "dist"

# Ensure SPA routes like /login are served by index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

